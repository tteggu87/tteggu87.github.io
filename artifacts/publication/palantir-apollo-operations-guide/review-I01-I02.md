# I01 · I02 실제 이미지 검토

검토 범위: 지정된 최종 PNG 2개와 기존 `previews/I01-390.png`, `previews/I02-390.png`만 native `view_image`로 확인했다. 이미지 생성·수정 및 다른 자료 열람은 하지 않았다. 아래 판정은 실제 표시 내용과 사용자가 제시한 기준에 한정한다.

## I01 — PASS

- 실제 문구: “Apollo, 클라우드 밖의 운영”, “Release · 정책”, “Hub · 변경 계획”, “Plan 조회 · 상태 보고”, “클라우드 · 온프레미스 · 엣지”, “승인된 Bundle 반입”, “격리 환경의 Hub”, “권한 · 아티팩트 · 관측 · 복구”, “배포를 통합해도 환경의 책임은 남습니다”.
- 전체 주제 범위: 릴리스·정책부터 변경 계획, 여러 실행 환경, 격리 환경, 운영 책임까지 포함한다. 단일 설치 요건에 치우치지 않아 전체 주제용 인포그래픽으로 통과한다.
- Airgap 분리: 오른쪽 격리 환경은 주황색 경계와 별도 Hub로 구분된다. 왼쪽 Hub의 청록색 연결은 왼쪽 실행 환경에서 끝난다. 격리 경계를 통과하는 표시는 상자와 “승인된 Bundle 반입” 화살표이며, 외부 Hub에서 격리 Hub로 이어지는 상시 네트워크 연결은 보이지 않는다. 오해를 유발하는 직접 연결은 관찰되지 않았다.
- 390px: 제목과 하단 핵심 명제는 읽힌다. 환경 구분과 운영 책임 문구도 식별된다. “Release · 정책”, “승인된 Bundle 반입”, “격리 환경의 Hub”는 매우 작아 세부 독해가 편하지 않지만, 분리된 영역과 반입 표현을 포함한 전체 구조 및 핵심 명제는 유지된다.
- 잔여 이슈: 모바일 폭에서 보조 라벨 가독성 여유가 적다. 핵심 명제의 판독 불가나 사실 실패로 판정할 정도는 아니다.

## I02 — FAIL

- 실제 문구: “Kubernetes Spoke의 설치 범위”, “Hub”, “Plan 조회 · 상태 보고”, “Spoke Control Plane”, “전체 권장 10 vCPU · 16GB”, “노드당 수치 아님 · 업무 자원 별도”, “네트워크 · 저장소”, “환경 keypair”, “권한 검토”, “Kubernetes Spoke 기준, 모든 엣지의 최소치가 아닙니다”.
- 전체 권장 자원 범위: PASS. 세 서버를 하나의 중괄호로 묶고 그 아래 “전체 권장 10 vCPU · 16GB”를 표시한다. “노드당 수치 아님”도 명시되어 제어 평면 전체 권장량이라는 의미가 분명하다.
- 최소 3개 및 x86_64: FAIL. 서버 그림은 3개지만 “최소 3개” 또는 이에 해당하는 문구가 없다. 정확히 3개인 예시와 최소 요건을 구별할 수 없다. `x86_64` 표기는 최종 이미지와 390px 미리보기 어디에도 보이지 않는다. 필수 요건 누락이며, 중립적인 추가 라벨 때문에 내린 실패 판정이 아니다.
- 390px: 제목, 제어 평면 라벨, 전체 권장 수치, 노드당 수치가 아니라는 주의 문구, 하단 적용 범위 명제는 읽힌다. “Plan 조회 · 상태 보고”는 작지만 식별 가능하다. 핵심 명제 가독성은 통과한다.
- 잔여 이슈: 최소 3개 노드와 x86_64 아키텍처 요건을 실제 텍스트로 명시해야 요청된 기준을 충족한다. 현재 그림만으로 두 요건을 충족했다고 볼 수 없다.

## 3줄 요약

I01 PASS: 전체 주제와 airgap 분리가 표현되며 390px에서도 핵심 명제가 읽힌다.
I02 FAIL: 전체 권장 10 vCPU·16GB 및 노드당 아님은 명확하지만 최소 3개·x86_64 문구가 없다.
잔여 이슈: I01 보조 라벨은 작고, I02는 필수 요건 표기가 누락되어 있다. 이미지 변경은 하지 않았다.
